# Runner Server Port
PORT=3002

# Allowed Script Directories (comma-separated)
# Additional directories where test scripts can be located.
# Paths can be absolute or relative (resolved from runner's working directory).
# The runner's own 'uploads' directory is always allowed by default.
# Default also includes '../backend/uploads/ui-tests' for development convenience.
#
# Example:
#   ALLOWED_SCRIPT_DIRS=/app/backend/uploads/ui-tests,/shared/scripts
ALLOWED_SCRIPT_DIRS=../backend/uploads/ui-tests

# Node Environment
NODE_ENV=development
